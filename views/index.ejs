<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="index.css">
</head>

<body>
    <h1 class="heading">
        <%= title %>
    </h1>

    <a href="/create" class="addEmp">Add an Employee</a>
    <% if(locals.user){ %>
        <% user.forEach(users=>{ %>
            <div class="indexContainer">

                <div class="detail">
                    <span>Name</span>
                    <span>: <%= users.first_name %>
                            <%= users.last_name %></span>
                </div>

                <div class="detail">
                    <span>DATE OF BIRTH</span>
                    <span>: <%= users.dob %></span>
                </div>

                <div class="detail">
                    <span>Phone</span>
                    <span>: <%= users.phone_no %></span>
                </div>

                <div class="detail">
                    <span>Email</span>
                    <span>: <%= users.email %></span>
                </div>

                <div class="detail">
                    <span>ABOUT</span>
                    <span>: <%= users.bio %></span>
                </div>


                    <form action="/<%= users.id %>/delete" method="post" style="display: inline;">
                        <a href="/<%= users.id %>/update" class="indexUpBtn">UPDATE</a>
                        <button type="submit" onclick="return confirm('Are you sure you want to delete this user?')"
                            class="deleteBtn">
                            DELETE
                        </button>
                    </form>

                    
            </div>
            <% }) %>
                <% } %>
</body>

</html>